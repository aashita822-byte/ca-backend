# Core API
fastapi
uvicorn
python-multipart

# MongoDB (Atlas-safe)
pymongo[srv]
motor

# Auth & Security
python-jose[cryptography]
passlib[bcrypt]

# HTTP
httpx>=0.24

# Data validation
pydantic
pydantic-settings
python-dotenv

# Vector DB
pinecone

# PDF text extraction (TEXT ONLY – stable)
pypdf>=3.15
pdfplumber>=0.10

# Images (for future use, safe)
Pillow>=9.5.0
pytesseract>=0.3.10
